# Tesla Model 3 PCS (US): запуск зарядки без charge port / EVSE

Проект настроен для 32-амперного однофазного PCS с американской Tesla Model 3.
Контроллер эмулирует сообщения американского Type-1/NACS charge port и
автоматически делает запрос зарядки, когда сам PCS подтверждает наличие сети в
кадре `0x264`.

> Высокое напряжение и сеть 220 В опасны для жизни. Первый запуск выполняйте с
> ограничением тока, предохранителями, межблокировкой, корректным precharge и
> возможностью аппаратно снять AC/HV. Прошивка не заменяет эти защиты.

## Что показал приложенный `can.trc.txt`

Файл является агрегированным снимком (одно значение и счётчик для каждого ID),
поэтому он не показывает порядок событий. Тем не менее в нём есть несколько
однозначных проблем.

| Кадр | Что было в логе | Расшифровка / проблема |
|---|---|---|
| `0x264` | `C3 19 80 00 40 01` | 220 В, **0 А**, 0 Вт, лимит PCS 32 А. Показание 51 А было ошибкой декодера STM32. |
| `0x204` | `78 00 00 FF 00 00 00 09` | Main state `8 = faulted`, HV charge status `3 = faulted`, single phase; вход CHG/PWM виден PCS как активный. |
| `0x2B2` | `00 20 C9`, DLC 3 | Некорректный запрос мощности. Старый код передавал в HAL число как адрес массива, поэтому в CAN попадали случайные байты памяти. |
| `0x22A` | `00 0B 7F 13 00 00 00 00`, DLC 8 | Более поздний стендовый тест подтвердил для этого PCS длинный вариант с запросом precharge `0x0B00`; текущий код сохраняет DLC 8 и исправляет режим работы. |
| `0x23D` | `00 00` | Charge-port emulator сообщал допустимый ток 0 А. |
| `0x2C4` | `2C 01 90 01 00 00 00 00`, count 829 | Контроллер ошибочно передавал ID, который должен приходить от PCS. Это создавало коллизию и скрывало реальное измерение HV. |

Главный вывод: `ACamps=51` не является током, SNA или признаком потребления.
Непосредственная причина отсутствия зарядки — PCS находится в faulted, получая
несогласованные команды зарядки.

## Что исправлено

- Исправлен декодер `0x264`: ток берётся из битов 14…22 с маской 9 бит.
- Устранено повреждение `0x2B2`; теперь отправляется настоящий массив байтов.
- Для этого PCS сначала используется подтверждённый длинный `0x2B2` DLC 5.
  Если PCS выдаст CAN-rationality alert `0x1E`, код переключит длину 3/5
  автоматически.
- `0x22A` имеет DLC 8, проверенный запрос precharge `0x0B00`, режим
  support/charge и реальное свежее напряжение HV.
- Контроллер больше не передаёт `0x2C4`; он только принимает этот кадр от PCS.
- `0x23D` передаётся в post-2020 формате DLC 4 (`05 20 FF 0F` при активной
  зарядке 16 А); `0x21D` и `0x25D` эмулируют подключённый американский
  Type-1/NACS port.
- Добавлен post-2020 кадр `0x13D` с DLC 6 и тем же лимитом 16 А.
- CHG включается только после свежих CAN, HV и AC данных и отсутствия faulted.
- Добавлен автоматический старт от подтверждённых PCS 180 В или выше:
  1 секунда стабилизации, затем 0,5 секунды проверки готовности.
- Запрос мощности начинается с 0 Вт и растёт по 10 Вт каждые 100 мс до
  `ACvolts × CHGcurrentSetpointA` (по умолчанию 16 А, не более 4000 Вт).
  При уменьшении уставки мощность снижается по 100 Вт каждые 100 мс.
- Добавлены приём и разбор `0x424` (alert log), расширенная диагностика `0x204`
  и контроль свежести всех критичных измерений.
- Макросы полярности выходов теперь действительно учитывают настройки
  `*_ACTIVE_LEVEL`.

## Настройки первого запуска

Настройки находятся в `Core/Src/main.c` и `Core/Inc/defines.h`.

| Параметр | Значение | Назначение |
|---|---:|---|
| `USpcs` | `0` | Выбор длинного `0x2B2` для конкретного американского PCS; профиль charge port при этом остаётся US |
| `CHGcurrentSetpointA` | 16 А | Главная изменяемая уставка 0…16 А |
| `ACILim` | 16 А | Применённый после ограничения ток в `0x13D`, `0x21D`, `0x23D`, `0x333` |
| `CHGpwrTarget` | вычисляется | `ACvolts × CHGcurrentSetpointA`, максимум 4000 Вт |
| `PCS_AC_START_MIN_V` | 180 В | Порог автоматического старта |
| `PCS_AC_HOLD_MIN_V` | 80 В | Порог удержания с гистерезисом |
| `PCS_HV_MIN_V` / `MAX_V` | 276 / 420 В | Допустимое свежее измерение HV |
| `AUTO_CHARGE_FROM_AC` | `1` | Автозапуск без физического charge port |

Перед подачей AC обязательно измерьте уровни непосредственно на разъёме PCS:

- `PCS_ENABLE`, `DCDC` и `CHG` должны соответствовать вашей выходной схеме;
- у оригинальной V1 controller board линии DCDC/CHG инвертированы транзисторами,
  а PCS ожидает активные 5 В;
- для прямого неинвертирующего level shifter оставьте `GPIO_PIN_SET`;
- для инвертирующего каскада измените только соответствующий
  `*_ENABLE_ACTIVE_LEVEL` на `GPIO_PIN_RESET`.

В приложенном логе `0x204` сообщает `P=1`, то есть PCS видел линию CHG/PWM
активной. Это хороший признак, но напряжение на пине всё равно нужно проверить.

## Сборка и прошивка

Из корня проекта:

```sh
make test
make -j4
```

Готовые файлы:

- `build/tesla_charger.bin` — для STM32CubeProgrammer;
- `build/tesla_charger.elf` — для SWD debug;
- `build/tesla_charger.map` — карта памяти.

Проверенная сборка: Flash 44 672 / 65 536 байт, RAM 3 592 / 20 480 байт.
SHA-256 BIN:
`64b52c8c426736cea21e2055601c639d34aee7900f5c6e94046ea3deff1fb602`.

Если используется STM32CubeIDE, обновите дерево проекта и сделайте Clean/Build,
чтобы IDE добавила новые `pcs_protocol.c/.h`. Надёжный контрольный вариант —
корневой `Makefile`, который уже проверен полной линковкой.

## Безопасная последовательность проверки

1. После старой faulted-сессии полностью снимите AC и штатно обесточьте PCS,
   выдержав время разряда DC-link согласно вашей аппаратной схеме.
2. Прошейте новый `build/tesla_charger.bin`.
3. Пока AC и HV отключены, проверьте CAN 500 кбит/с, общий провод, терминаторы и
   физические уровни `PCS_ENABLE`, `DCDC`, `CHG`.
4. Включите контроллер/PCS. Убедитесь, что `QOV=0`, `ERR=0`, сообщения PCS
   принимаются, а искусственного TX `0x2C4` больше нет.
5. Подайте HV только через проверенную схему precharge. Дождитесь свежего
   корректного `0x2C4` и допустимого `U hv`.
6. Подайте 220 В через отдельную аппаратную защиту и ограничение тока.
7. Через примерно 1,5 секунды автомат должен пройти состояния 1 → 2 → 3.
   Набор от 0 Вт идёт со скоростью 100 Вт/с; при 213 В и 16 А достижение
   3408 Вт занимает около 34 секунд, после чего будет STATE 4.
   Кнопка UP остаётся ручным stop/start; после ручного stop автомат повторно
   запустится только после цикла AC off/on.
8. Для немедленной остановки снимите AC аппаратно; не полагайтесь только на CAN.

После прошивки ожидаемые управляющие кадры при лимите 16 А:

| ID | Standby | Во время зарядки |
|---|---|---|
| `0x13D`, DLC 6 | `0A 20 AA 1A FF 02` | `05 20 AA 1A FF 02` |
| `0x21D`, DLC 8 | `5D 20 00 10 80 00 60 10` | такой же US connected profile |
| `0x23D`, DLC 4 | `0A 20 FF 0F` | `05 20 FF 0F` |
| `0x2B2`, DLC 5 | `00 00 00 00 00` | мощность LE + `02 00 00`; при 213 В и 16 А: `50 0D 02 00 00` |
| `0x22A`, DLC 8 | `00 0B`, HV и режим support | `00 0B`, HV и режим DCDC+charge |

Значение двух старших байтов `0x22A` зависит от реального HV. Например при
319 В активный DCDC+CHG кадр будет `00 0B FD 13 00 00 00 00`.

## Экран и состояния

| `STATE` | Значение |
|---:|---|
| 0 | Boot, задержка пробуждения |
| 1 | Standby |
| 2 | Ожидание свежих CAN/HV/AC и готовности PCS |
| 3 | Плавный набор мощности |
| 4 | Зарядка на целевой мощности |
| 10 | Нет свежего ответа PCS / CAN fault |
| 11 | Нет свежего `0x2C4` или HV вне 276…420 В |
| 12 | Нет свежего `0x264` или AC пропало |
| 13 | PCS сообщил charger faulted в `0x204` |

Нижняя диагностическая строка:

- `264`, `224`, `2C4` — получены соответствующие кадры;
- `M` — main state из `0x204`;
- `H` — HV charge status (`0=standby`, `1=blocked`, `2=enabled`, `3=faulted`);
- `G` — grid config (`1=single phase`);
- `P` — состояние аппаратной линии CHG/PWM, которое видит PCS;
- `AL` — последний alert ID из `0x424`.

В SWD Watch полезны:

```text
controlState, chg_request, CHGpwr, ACvolts, ACamps, ACpwr, HVvolts
pcs_main_state, pcs_hv_charge_status, pcs_grid_config, pcs_pwm_enable_line
pcs_last_alert_id, pcs_alert_can_id, pcs_alert_rx_error
dbg_tx22A, dbg_tx2B2, dbg_tx2B2_dlc, dbg_tx23D[0..3]
canTxErrors, canTxQueueOverflows, schedulerMisses
```

## Как снять следующий полезный CAN-лог

Нужен не агрегированный список, а хронологический trace с timestamp и, если
адаптер умеет, направлением TX/RX:

1. Начать запись при AC off.
2. Записать 3–5 секунд standby.
3. Подать AC и записывать не менее 60 секунд, чтобы увидеть весь набор мощности.
4. Не объединять одинаковые ID в одну строку со счётчиком.
5. Сохранить минимум ID `13D, 204, 21D, 22A, 23D, 264, 2B2, 2C4, 3A4,
   424, 76C`; лучше сохранить всю IPC CAN шину.

Если зарядка не стартует, первым делом смотрите:

- `STATE=11`: появился ли настоящий свежий `0x2C4`, совпадает ли HV с
  мультиметром/изоляционным измерением;
- `STATE=13`: значения `M`, `H`, `AL`, затем точный `0x424`;
- `AL=30` (`0x1E`): `pcs_alert_can_id` укажет отвергнутый ID, а
  `pcs_alert_rx_error` — тип ошибки длины/рациональности;
- state 3/4, но 0 А: проверить реальный CHG=5 В на PCS, кадры `0x2B2/0x22A`,
  AC contactor/fuse и динамику `0x204/0x264/0x76C`.

## Источники протокола

- Damien Maguire, Tesla Model 3 Charger, US PCS charge tested:
  https://github.com/damienmaguire/Tesla-Model-3-Charger
- Проверенный US V6 набор кадров:
  https://github.com/damienmaguire/Tesla-Model-3-Charger/blob/master/Software/M3_PCS_V6.ino
- Актуальный STM32 PCS controller / alert decoding:
  https://github.com/damienmaguire/Tesla-Model-3-Charger/blob/master/Software/stm32-M3-PCS/src/PCSCan.cpp
- Model 3 DBC (битовые поля `0x204`, `0x21D`, `0x22A`, `0x264`):
  https://github.com/joshwardell/model3dbc/blob/master/Model3CAN.dbc
