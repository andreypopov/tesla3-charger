# Tesla Model 3 PCS 32A single-phase: запуск без charge port / EVSE

Проект настроен для 32-амперного однофазного PCS Tesla Model 3. Контроллер
эмулирует сообщения американского Type-1/NACS charge port и автоматически
делает запрос зарядки, когда сам PCS подтверждает наличие сети в кадре `0x264`.
Требуемый этим блоком длинный `0x2B2` DLC 5 указывает на поколение протокола,
но сам по себе не определяет регион PCS.

> Высокое напряжение и сеть 220 В опасны для жизни. Первый запуск выполняйте с
> ограничением тока, предохранителями, межблокировкой, корректным precharge и
> возможностью аппаратно снять AC/HV. Прошивка не заменяет эти защиты.

## Что показал приложенный `can.trc.txt`

Файл является агрегированным снимком (одно значение и счётчик для каждого ID),
поэтому он не показывает порядок событий. Тем не менее в нём есть несколько
однозначных проблем.

| Кадр | Что было в логе | Расшифровка / проблема |
|---|---|---|
| `0x264` | `C3 19 80 00 40 01` | 220 В, **0 А**, 0 Вт, лимит PCS 32 А. Показание 51 А было ошибкой декодера STM32. |
| `0x204` | `78 00 00 FF 00 00 00 09` | Main state `8 = faulted`, HV charge status `3 = faulted`, single phase; вход CHG/PWM виден PCS как активный. |
| `0x2B2` | `00 20 C9`, DLC 3 | Некорректный запрос мощности. Старый код передавал в HAL число как адрес массива, поэтому в CAN попадали случайные байты памяти. |
| `0x22A` | `00 0B 7F 13 00 00 00 00`, DLC 8 | Более поздний стендовый тест подтвердил для этого PCS длинный вариант с запросом precharge `0x0B00`; текущий код сохраняет DLC 8 и исправляет режим работы. |
| `0x23D` | `00 00` | Charge-port emulator сообщал допустимый ток 0 А. |
| `0x2C4` | `2C 01 90 01 00 00 00 00`, count 829 | Контроллер ошибочно передавал ID, который должен приходить от PCS. Это создавало коллизию и скрывало реальное измерение HV. |

Главный вывод: `ACamps=51` не является током, SNA или признаком потребления.
Непосредственная причина отсутствия зарядки — PCS находится в faulted, получая
несогласованные команды зарядки.

## Что показал снимок `3.txt` после RC3

Это тоже агрегированный снимок, но устойчивый рабочий режим читается
однозначно:

| Кадр | Значение | Вывод |
|---|---|---|
| `0x23D` | `05 20 FF 0F` | Новый post-2020 формат действительно прошит и принимается; сам по себе он не снял ограничение 8 А. |
| `0x264` | `D2 19 94 12 A0 00` | Около 220 В, 8,0 А, 1,8 кВт; PCS сообщает общий лимит 16 А. |
| `0x2B2` | `C0 0D 02 00 00` | Контроллер корректно запрашивает 3520 Вт, то есть 220 В × 16 А. |
| `0x204` | `66 04 03 FF 00 00 50 09` | Зарядка разрешена, однофазный 32-амперный US PCS включает фазу C, но сам задаёт ей только 8,0 А. |
| `0x21D` | `5D 20 00 10 80 00 60 10` | Pilot равен 16 А, однако отдельная capability кабеля тоже была занижена до 16 А. |
| `0x333` | `04 10 29 07` | Отдельный UI-предел также был 16 А вместо штатных 48 А из проверенного профиля. |

RC4 затем убрал эти два возможных ограничения: `0x21D` объявил кабель 32 А, а
`0x333` — UI capability 48 А. Снимок `4.txt` подтвердил, что кадры изменились,
но ток остался 8 А. Значит, capability-поля тоже исключены.

## Что показал снимок `4.txt` после RC4

| Кадр | Значение | Вывод |
|---|---|---|
| `0x21D` | `5D 20 00 20 80 00 60 10` | RC4 действительно прошит: pilot 16 А, capability кабеля 32 А. |
| `0x333` | `04 30 29 07` | UI capability действительно 48 А. |
| `0x264` | `7E 19 94 11 A0 00` | Около 217 В, ровно 8,0 А, 1,7 кВт; лимит PCS 16 А. |
| `0x2B2` | `90 0D 02 00 00` | Запрошено 3472 Вт, то есть 217 В × 16 А. |
| `0x204` | `66 04 03 FF 00 00 50 09` | Заряд разрешён; аппаратный вариант `1 = 32A single-phase`; фаза C запрашивает 8,0 А. |
| `0x3A4` | `00 00 00 08 07 00 00 00` | Страница alert matrix 0; прежнего `CP_MIA` больше нет. |
| `0x2B4` | `6C 31 21 79 00` | DC/DC реально работает: около 14,2 В, 311 В HV и 12,1 А на LV-выходе. |

Новая закономерность воспроизводится в двух логах с разным напряжением:

```text
4.txt: 3472 Вт / 217 В / 2 = 8,0 А
3.txt: 3520 Вт / 220 В / 2 = 8,0 А
```

Именно этот hardware variant 1 ведёт расчёт так, как будто общий запрос
`0x2B2` делится между двумя внутренними каналами. RC5 проверяет эту гипотезу,
кодируя удвоенную мощность только для variant 1.
Это ещё требует аппаратного подтверждения; токовые кадры остаются ограничены
16 А.

## Что исправлено

- Исправлен декодер `0x264`: ток берётся из битов 14…22 с маской 9 бит.
- Устранено повреждение `0x2B2`; теперь отправляется настоящий массив байтов.
- Для этого PCS сначала используется подтверждённый длинный `0x2B2` DLC 5.
  Если PCS выдаст CAN-rationality alert `0x1E`, код переключит длину 3/5
  автоматически.
- `0x22A` имеет DLC 8, проверенный запрос precharge `0x0B00`, режим
  support/charge и реальное свежее напряжение HV.
- Контроллер больше не передаёт `0x2C4`; он только принимает этот кадр от PCS.
- `0x23D` передаётся в post-2020 формате DLC 4 (`05 20 FF 0F` при активной
  зарядке 16 А); `0x21D` и `0x25D` эмулируют подключённый американский
  Type-1/NACS port.
- Добавлен post-2020 кадр `0x13D` с DLC 6 и тем же лимитом 16 А.
- После снимка `3.txt` разделены фактическая уставка и паспортные возможности:
  pilot `0x21D` остаётся 16 А, но cable capability `0x21D` равен 32 А, а
  штатный UI capability `0x333` — 48 А. Это повторяет проверенный US-профиль и
  не увеличивает фактический запрос выше 16 А.
- После снимка `4.txt` добавлена компенсация hardware variant 1: физическая
  цель остаётся `ACvolts × ток`, а кодированное значение `0x2B2` умножается на
  2. Для остальных вариантов множитель равен 1.
- Все живые токовые ограничения (`0x13D`, pilot `0x21D`, `0x23D`) и регистр
  `CHGcurrentSetpointA` по-прежнему жёстко ограничены 16 А.
- Добавлен независимый программный guard: если свежий `0x264` показывает ток
  более чем на 2 А выше уставки, CHG немедленно выключается и фиксируется
  `STATE=14`. Аппаратный автомат и предохранитель всё равно обязательны.
- Добавлен правильный декодер DC/DC rail status `0x2B4`; нулевое legacy-поле
  тока из `0x224` больше не затирает фактические 12,1 А из нового лога.
- CHG включается только после свежих CAN, HV и AC данных и отсутствия faulted.
- Добавлен автоматический старт от подтверждённых PCS 180 В или выше:
  1 секунда стабилизации, затем 0,5 секунды проверки готовности.
- Запрос мощности начинается с 0 Вт. Физическая цель растёт примерно по
  100 Вт/с до `ACvolts × CHGcurrentSetpointA` (по умолчанию 16 А, максимум
  4000 Вт). При variant 1 передаваемое значение растёт вдвое быстрее и имеет
  верх 8000 кодированных ватт, сохраняя ту же физическую скорость набора.
- Добавлены приём и разбор `0x424` (alert log), расширенная диагностика `0x204`
  и контроль свежести всех критичных измерений.
- Макросы полярности выходов теперь действительно учитывают настройки
  `*_ACTIVE_LEVEL`.

## Настройки первого запуска

Настройки находятся в `Core/Src/main.c` и `Core/Inc/defines.h`.

| Параметр | Значение | Назначение |
|---|---:|---|
| `PCS_2B2_START_SHORT` | `0` | Старт с подтверждённого длинного `0x2B2` DLC 5; это настройка формата, не региона |
| `CHARGE_VARIANT1_COMPENSATION` | `1` | Разрешить множитель мощности ×2 только при `pcs_hw_variant=1` |
| `CHGcurrentSetpointA` | 16 А | Главная изменяемая уставка 0…16 А |
| `CHARGE_OVERCURRENT_MARGIN_A` | 2 А | Допуск программного аварийного отключения по свежему `0x264` |
| `ACILim` | 16 А | Применённый после ограничения ток в `0x13D`, pilot `0x21D` и `0x23D` |
| `CHARGE_CABLE_CAPABILITY_A` | 32 А | Паспортная возможность кабеля в байте 3 `0x21D`; не команда зарядного тока |
| `CHARGE_UI_CAPABILITY_A` | 48 А | Штатный верхний UI-предел в `0x333`; не команда зарядного тока |
| `CHGdesiredPowerTargetW` | вычисляется | Физическая цель `ACvolts × CHGcurrentSetpointA`, максимум 4000 Вт |
| `CHGpowerRequestMultiplier` | 1 или 2 | Фактически выбранный множитель по hardware variant |
| `CHGpwrTarget` | вычисляется | Цель внутри `0x2B2`, максимум 8000 кодированных ватт |
| `PCS_AC_START_MIN_V` | 180 В | Порог автоматического старта |
| `PCS_AC_HOLD_MIN_V` | 80 В | Порог удержания с гистерезисом |
| `PCS_HV_MIN_V` / `MAX_V` | 276 / 420 В | Допустимое свежее измерение HV |
| `AUTO_CHARGE_FROM_AC` | `1` | Автозапуск без физического charge port |

Перед подачей AC обязательно измерьте уровни непосредственно на разъёме PCS:

- `PCS_ENABLE`, `DCDC` и `CHG` должны соответствовать вашей выходной схеме;
- у оригинальной V1 controller board линии DCDC/CHG инвертированы транзисторами,
  а PCS ожидает активные 5 В;
- для прямого неинвертирующего level shifter оставьте `GPIO_PIN_SET`;
- для инвертирующего каскада измените только соответствующий
  `*_ENABLE_ACTIVE_LEVEL` на `GPIO_PIN_RESET`.

В приложенном логе `0x204` сообщает `P=1`, то есть PCS видел линию CHG/PWM
активной. Это хороший признак, но напряжение на пине всё равно нужно проверить.

## Сборка и прошивка

Из корня проекта:

```sh
make test
make -j4
```

Готовые файлы:

- `build/tesla_charger.bin` — для STM32CubeProgrammer;
- `build/tesla_charger.elf` — для SWD debug;
- `build/tesla_charger.map` — карта памяти.

Проверенная сборка RC5: Flash 45 900 / 65 536 байт, RAM 3 632 / 20 480 байт.
SHA-256 BIN:
`24933c421e705a28747902569711d55bd612073f639d61a0e4948014adc55e4f`.

Если используется STM32CubeIDE, обновите дерево проекта и сделайте Clean/Build,
чтобы IDE добавила новые `pcs_protocol.c/.h`. Надёжный контрольный вариант —
корневой `Makefile`, который уже проверен полной линковкой.

## Безопасная последовательность проверки

1. После старой faulted-сессии полностью снимите AC и штатно обесточьте PCS,
   выдержав время разряда DC-link согласно вашей аппаратной схеме.
2. Прошейте новый `build/tesla_charger.bin`.
3. Пока AC и HV отключены, проверьте CAN 500 кбит/с, общий провод, терминаторы и
   физические уровни `PCS_ENABLE`, `DCDC`, `CHG`.
4. Включите контроллер/PCS. Убедитесь, что `QOV=0`, `ERR=0`, сообщения PCS
   принимаются, а искусственного TX `0x2C4` больше нет.
5. Подайте HV только через проверенную схему precharge. Дождитесь свежего
   корректного `0x2C4` и допустимого `U hv`.
6. Подайте 220 В через отдельную аппаратную защиту и ограничение тока.
7. Для первого RC5-теста рекомендуется через SWD задать
   `CHGcurrentSetpointA=10`, проверить `I set/PCS=10/10`, реальный ток и только
   затем перейти к 16 А. Через примерно 1,5 секунды автомат должен пройти
   состояния 1 → 2 → 3. Физический набор идёт со скоростью около 100 Вт/с;
   при 217 В и 16 А достижение цели 3472 Вт занимает около 35 секунд. На шине
   при этом будет плавно достигнуто 6944 кодированных ватта.
   Кнопка UP остаётся ручным stop/start; после ручного stop автомат повторно
   запустится только после цикла AC off/on.
8. Немедленно снимите AC аппаратно, если измеренный ток превысил уставку,
   появился `0x424` или PCS перешёл в faulted. Не полагайтесь только на CAN.

После прошивки ожидаемые управляющие кадры при лимите 16 А:

Заголовок экрана этой тестовой версии: `PCS RC5`.

| ID | Standby | Во время зарядки |
|---|---|---|
| `0x13D`, DLC 6 | `0A 20 AA 1A FF 02` | `05 20 AA 1A FF 02` |
| `0x21D`, DLC 8 | `5D 20 00 20 80 00 60 10` | `5D 20 00 20 80 00 60 10` |
| `0x23D`, DLC 4 | `0A 20 FF 0F` | `05 20 FF 0F` |
| `0x333`, DLC 4 | `04 30 29 07` | `04 30 29 07` |
| `0x2B2`, DLC 5 | `00 00 00 00 00` | при variant 1: физическая цель ×2, LE + `02 00 00`; при 217 В и 16 А: `20 1B 02 00 00` |
| `0x22A`, DLC 8 | `00 0B`, HV и режим support | `00 0B`, HV и режим DCDC+charge |

Значение двух старших байтов `0x22A` зависит от реального HV. Например при
319 В активный DCDC+CHG кадр будет `00 0B FD 13 00 00 00 00`.
В `0x21D` байт 1 (`0x20`) остаётся живым pilot-лимитом 16 А, а байт 3
(`0x20`) обозначает отдельную 32-амперную возможность кабеля. `0x333` с 48 А
не отменяет фактические пределы 16 А в остальных командах.

## Экран и состояния

| `STATE` | Значение |
|---:|---|
| 0 | Boot, задержка пробуждения |
| 1 | Standby |
| 2 | Ожидание свежих CAN/HV/AC и готовности PCS |
| 3 | Плавный набор мощности |
| 4 | Зарядка на целевой мощности |
| 10 | Нет свежего ответа PCS / CAN fault |
| 11 | Нет свежего `0x2C4` или HV вне 276…420 В |
| 12 | Нет свежего `0x264` или AC пропало |
| 13 | PCS сообщил charger faulted в `0x204` |
| 14 | Измеренный AC-ток более чем на 2 А превысил заданный |

Нижняя диагностическая строка:

- `264`, `224`, `2C4` — получены соответствующие кадры;
- `M` — main state из `0x204`;
- `H` — HV charge status (`0=standby`, `1=blocked`, `2=enabled`, `3=faulted`);
- `G` — grid config (`1=single phase`);
- `P` — состояние аппаратной линии CHG/PWM, которое видит PCS;
- `AL` — последний alert ID из `0x424`.

Строка `I set/PCS` теперь показывает две разные величины: слева фактическая
уставка контроллера, справа округлённый внутренний запрос активной фазы из
`0x204`. Для американского однофазного PCS обычно активна фаза C. В снимке
`3.txt` кадр `0x204 = 66 04 03 FF 00 00 50 09` поэтому показывает `16/8`:
контроллер просит 16 А, а PCS сознательно ограничивает себя до 8,0 А.

Строка `P/CAN` показывает физическую цель мощности, текущий передаваемый
setpoint `0x2B2` и множитель. Для лога `4.txt` на установившемся режиме RC5
должен показать примерно `3472/6944x2`. Если справа по-прежнему будет
`I set/PCS=16/8`, гипотеза не подтвердилась и повышать множитель дальше нельзя.

`DC-DC amps` и `U lv` теперь берутся из `0x2B4`. В `4.txt` это примерно 12 А
и 14 В (сырые значения 121 = 12,1 А и 364 = 14,21875 В). Поэтому этот снимок
не подтверждает неисправность DC/DC — наоборот, он показывает его работу.

В SWD Watch полезны:

```text
controlState, chg_request, CHGdesiredPowerTargetW, CHGpwrTarget, CHGpwr
CHGpowerRequestMultiplier, ACvolts, ACamps, ACpwr, HVvolts, LVvolts, DCDCamps
pcs_main_state, pcs_hv_charge_status, pcs_grid_config, pcs_pwm_enable_line
pcs_phase_a_request_deci_amps, pcs_phase_b_request_deci_amps
pcs_phase_c_request_deci_amps, pcs_ac_current_request_amps
pcs_last_alert_id, pcs_alert_can_id, pcs_alert_rx_error
dcdc_lv_voltage_raw, dcdc_hv_voltage_raw, dcdc_output_current_raw
dcdc_legacy_current_raw, rx2B4Count, last2B4Ms, charge_overcurrent_trip_count
dbg_tx22A, dbg_tx2B2, dbg_tx2B2_dlc, dbg_tx23D[0..3]
dbg_tx21D[0..7], dbg_tx333[0..3]
canTxErrors, canTxQueueOverflows, schedulerMisses
```

## Как снять следующий полезный CAN-лог

Нужен не агрегированный список, а хронологический trace с timestamp и, если
адаптер умеет, направлением TX/RX:

1. Начать запись при AC off.
2. Записать 3–5 секунд standby.
3. Подать AC и записывать не менее 60 секунд, чтобы увидеть весь набор мощности.
4. Не объединять одинаковые ID в одну строку со счётчиком.
5. Сохранить минимум ID `13D, 204, 21D, 22A, 23D, 264, 2B2, 2B4, 2C4,
   3A4, 424, 76C`; лучше сохранить всю IPC CAN шину.

Если зарядка не стартует, первым делом смотрите:

- `STATE=11`: появился ли настоящий свежий `0x2C4`, совпадает ли HV с
  мультиметром/изоляционным измерением;
- `STATE=13`: значения `M`, `H`, `AL`, затем точный `0x424`;
- `AL=30` (`0x1E`): `pcs_alert_can_id` укажет отвергнутый ID, а
  `pcs_alert_rx_error` — тип ошибки длины/рациональности;
- state 3/4, но 0 А: проверить реальный CHG=5 В на PCS, кадры `0x2B2/0x22A`,
  AC contactor/fuse и динамику `0x204/0x264/0x76C`.
- state 3/4 и снова ровно 8 А на RC5: проверить, что экран показывает `x2`, а
  `0x2B2` действительно около `20 1B 02 00 00`; множитель дальше не повышать.

## Источники протокола

- Damien Maguire, Tesla Model 3 Charger, US PCS charge tested:
  https://github.com/damienmaguire/Tesla-Model-3-Charger
- Проверенный US V6 набор кадров:
  https://github.com/damienmaguire/Tesla-Model-3-Charger/blob/master/Software/M3_PCS_V6.ino
- Актуальный STM32 PCS controller / alert decoding:
  https://github.com/damienmaguire/Tesla-Model-3-Charger/blob/master/Software/stm32-M3-PCS/src/PCSCan.cpp
- Model 3 DBC (битовые поля `0x204`, `0x21D`, `0x22A`, `0x264`, `0x2B4`):
  https://github.com/joshwardell/model3dbc/blob/master/Model3CAN.dbc
